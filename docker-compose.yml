services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: taskdb
      POSTGRES_USER: taskuser
      POSTGRES_PASSWORD: taskpass
      AUTH_DB: authdb
      AUTH_USER: authuser
      AUTH_PASSWORD: authpass
      COLLAB_DB: collabdb
      COLLAB_USER: collabuser
      COLLAB_PASSWORD: collabpass
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U taskuser -d taskdb"]
      interval: 10s
      timeout: 5s
      retries: 5

  auth-service:
    build: ./auth-service
    ports:
      - "4001:4001"
    environment:
      DATABASE_URL: postgresql://authuser:authpass@postgres:5432/authdb
      JWT_SECRET: your-super-secret-jwt-key-change-in-production
      PORT: 4001
    depends_on:
      postgres:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "--spider", "-q", "http://localhost:4001/health"]
      interval: 10s
      timeout: 5s
      retries: 3

  collaboration-service:
    build: ./collaboration-service
    ports:
      - "4006:4006"
    environment:
      DATABASE_URL: postgresql://collabuser:collabpass@postgres:5432/collabdb
      PORT: 4006
    depends_on:
      postgres:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "--spider", "-q", "http://localhost:4006/health"]
      interval: 10s
      timeout: 5s
      retries: 3

  task-service:
    build: ./task-service
    ports:
      - "8000:8000"
    environment:
      DATABASE_URL: postgresql://taskuser:taskpass@postgres:5432/taskdb
    depends_on:
      postgres:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8000/health')"]
      interval: 10s
      timeout: 5s
      retries: 3

  notification-service:
    build: ./notification-service
    ports:
      - "3001:3001"
    healthcheck:
      test: ["CMD", "wget", "--spider", "-q", "http://localhost:3001/health"]
      interval: 10s
      timeout: 5s
      retries: 3

  frontend:
    build:
      context: ./frontend
      args:
        VITE_TASK_API_URL: http://localhost:8000
        VITE_NOTIFICATION_API_URL: http://localhost:3001
        VITE_AUTH_API_URL: http://localhost:4001
        VITE_COLLAB_API_URL: http://localhost:4006
    ports:
      - "3000:80"
    depends_on:
      task-service:
        condition: service_healthy
      notification-service:
        condition: service_healthy
      auth-service:
        condition: service_healthy
      collaboration-service:
        condition: service_healthy

volumes:
  postgres_data:
