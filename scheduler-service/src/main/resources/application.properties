server.port=8002
spring.application.name=scheduler-service

# Service URLs
task.service.url=http://task-service:8000
notification.service.url=http://notification-service:3001

# Scheduling configuration
scheduler.task-reminder.enabled=true
scheduler.cleanup.enabled=true
scheduler.cleanup.older-than-days=7
