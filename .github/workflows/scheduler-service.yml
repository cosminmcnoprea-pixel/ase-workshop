name: Scheduler Service CI/CD

on:
  push:
    branches: [ main ]
    paths: [ 'scheduler-service/**' ]
  pull_request:
    branches: [ main ]
    paths: [ 'scheduler-service/**' ]

jobs:
  test:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Set up JDK 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'
    
    - name: Cache Maven packages
      uses: actions/cache@v3
      with:
        path: ~/.m2
        key: ${{ runner.os }}-m2-${{ hashFiles('**/pom.xml') }}
        restore-keys: ${{ runner.os }}-m2
    
    - name: Run tests
      working-directory: ./scheduler-service
      run: mvn test
    
    - name: Build JAR
      working-directory: ./scheduler-service
      run: mvn package -DskipTests

  build:
    needs: test
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main'
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Set up JDK 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'
    
    - name: Cache Maven packages
      uses: actions/cache@v3
      with:
        path: ~/.m2
        key: ${{ runner.os }}-m2-${{ hashFiles('**/pom.xml') }}
        restore-keys: ${{ runner.os }}-m2
    
    - name: Build JAR
      working-directory: ./scheduler-service
      run: mvn package -DskipTests
    
    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v3
    
    - name: Build Docker image
      uses: docker/build-push-action@v5
      with:
        context: ./scheduler-service
        platforms: linux/amd64
        tags: scheduler-service:${{ github.sha }}
        load: true
        
    # Uncomment below to push to GitHub Container Registry
    # - name: Log in to GitHub Container Registry
    #   uses: docker/login-action@v3
    #   with:
    #     registry: ghcr.io
    #     username: ${{ github.actor }}
    #     password: ${{ secrets.GITHUB_TOKEN }}
        
    # - name: Push to GitHub Container Registry
    #   uses: docker/build-push-action@v5
    #   with:
    #     context: ./scheduler-service
    #     platforms: linux/amd64
    #     tags: ghcr.io/${{ github.repository }}/scheduler-service:latest
    #     push: true
